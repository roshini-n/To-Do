# Frontend Environment Variables (optional)
# By default, the proxy in package.json routes to http://localhost:5000

# API Base URL (uses proxy from package.json if not set)
# REACT_APP_API_URL=http://localhost:5000
